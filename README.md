What is JSX, and why is it used?

Ans: It is syntax extension  that use can use react so that we can write like HTML code into JS.






What is the difference between State and Props?

Ans: State is own data that change with time. Props is sending data parent to child.






What is the useState hook, and how does it work?

Ans: useState is store and update internal state of componenet that hook of react. It is Update state with setValue(newvalue)





How can you share state between components in React?

Ans: state can share using lifting state up and sending props.





How is event handling done in React?

Ans:  in react, events are handle using camelCase props and passing function instead of string.












